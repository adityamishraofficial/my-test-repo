Your biggest blocker is at the very top of that file: **`"engines": {"node": ">=18"}`**. Angular 20 dropped Node 18 entirely — it needs `^20.19.0 || ^22.12.0 || ^24.0.0` (note: odd majors like 21/23 aren't supported). Sort that out before anything else, including CI images and any `.nvmrc`/Dockerfile.

## 1. Prep

```bash
git checkout -b feat/angular-20
node -v                      # must be 20.19+ / 22.12+
npm ls @angular/core         # confirm you're clean on 19.2.x first
```

Also: temporarily disable your `patch-package` postinstall. `patches/primeng+19.1.4.patch` will hard-fail the moment PrimeNG's version changes, and you don't want that masking real errors mid-update.

## 2. Core update

`ng update` refuses to run with unmet peers, and you have two known offenders (`angular2-multiselect-dropdown` pinned to ancient Angular, `jest-preset-angular` 14.x). Same play as last time:

```bash
npx ng update @angular/core@20 @angular/cli@20 --force
npx ng update @angular/cdk@20 2>/dev/null   # skip if unused
```

Then update the rest of the Angular-versioned packages manually — `@angular-devkit/build-angular`, `@angular/compiler-cli`, `@angular/language-service`, and **`ng-packagr` to ^20.x** (must match the major, or the library build breaks).

Keep `typescript` at `~5.8.3` — that's exactly what v20 wants. `zone.js ~0.15.1` and `rxjs ^7.8.1` are already fine.

## 3. Ecosystem bumps

| Package | 19 → 20 |
|---|---|
| `primeng` | ^19.1.4 → ^20.x (biggest job — see below) |
| `@fortawesome/angular-fontawesome` | ^1.0.0 → ^2.x |
| `@ngx-translate/core` | ^16.0.4 → ^17.x |
| `jest-preset-angular` | ^14.6.2 → ^15.x |
| `@angular-eslint/*` | ^19.8.1 → ^20.x |

Verify each against its own compat table rather than trusting me on the exact minor — I'm going from memory here.

Two warnings: `@angular-eslint` 20 pushes ESLint 9 + flat config, and you're on ESLint 8.57 (which is EOL anyway) with `.eslintrc`. That's its own PR — don't tangle it with the Angular jump. And `angular2-multiselect-dropdown` will never resolve peers; this is the upgrade to finally swap it for PrimeNG MultiSelect.

## 4. PrimeNG 20 — plan for real work

This is where most of your time goes:

- Theming moved from `@primeng/themes` to **`@primeuix/themes`** (you don't have the former listed, so check whether wings-front owns that config).
- The components deprecated in v19 are gone in v20: `p-dropdown` → `p-select`, `p-calendar` → `p-datepicker`, `p-inputSwitch` → `p-toggleswitch`, `p-overlayPanel` → `p-popover`, `p-sidebar` → `p-drawer`, `p-messages` → `p-message`, TabView → Tabs. Your AccordionTab migration already landed, so that one's paid off.
- Regenerate the patch: check first whether the `clearFilterValues()` type guard is still needed in 20.x. If it is, regenerate against the real 20.x tarball and keep `*.patch text eol=lf` in `.gitattributes` so it doesn't break on Git Bash again.

## 5. Library-specific (don't skip)

In `projects/ng-wings/package.json`: bump `peerDependencies` to `^20.0.0` and the version to `20.0.0`.

**Sequencing matters here.** A library built with ng-packagr 20 emits Angular 20 partial-Ivy output that an Angular 19 app *cannot* consume. So wings-front has to upgrade in lockstep — publish the v20 `ng-wings` to your internal registry only when wings-front's branch is ready to take it.

## 6. Expect these at build time

- `ng-reflect-*` attributes are gone in dev mode — any test asserting on them fails.
- `TestBed.flushEffects()` → `TestBed.tick()`.
- `platformBrowserDynamic()` is deprecated; `platformBrowser().bootstrapModule()` from `@angular/platform-browser` is the replacement. Deprecated, not removed, so it won't block you.
- `NgIf`/`NgFor`/`NgSwitch` are now soft-deprecated in favour of `@if`/`@for`. Optional, and `ng generate @angular/core:control-flow` will do it — but run it as a separate commit, not inside this one.

One last thing: v20 is in LTS now, not current. Doing 19→20 first is still correct (never skip a major), but budget for a follow-up.

Want me to sketch the exact `package.json` diff, or work through the PrimeNG 20 component migration list against your codebase?














All package.json edits via `npm pkg set` / `npm pkg delete` — no manual editing. You're on MINGW64, so single quotes work; these would need rewriting in PowerShell (`^` and `||` are escape chars there).

## Phase 0

```bash
node -v
git checkout -b feat/angular-20

npm run build 2>&1 | tee ../base-build.log
npm test 2>&1 | tee ../base-test.log
npm run build:showcase 2>&1 | tee ../base-showcase.log
```

Skip patch-package without editing anything — just add the flag to installs for the duration:

```bash
npm ci --ignore-scripts
```

(If you'd rather remove the hook: `npm pkg get scripts.postinstall` to record it, `npm pkg delete scripts.postinstall`, restore later with `npm pkg set scripts.postinstall="patch-package"`.)

## Phase 1

```bash
grep -rn "angular2-multiselect-dropdown" projects/ src/
npm uninstall angular2-multiselect-dropdown          # only if zero hits

npm pkg set version=20.0.0-SNAPSHOT
npm pkg set 'engines.node=^20.19.0 || ^22.12.0 || ^24.0.0'

npm config delete always-auth --location=project
```

## Phase 2

```bash
npx ng update @angular/core@20 @angular/cli@20 --force
```

That should carry the whole `@angular/*` group (compiler-cli, language-service, platform-browser*, router, forms…) plus `@angular-devkit/build-angular`. Verify rather than assume:

```bash
npx ng version
npm ls @angular/core
npm outdated | grep -E "@angular|ng-packagr"
```

Anything left behind, install explicitly:

```bash
npm i -D ng-packagr@^20 @angular-devkit/build-angular@^20
npm i -D @angular/compiler-cli@^20 @angular/language-service@^20
```

`ng-packagr` may or may not respond to `ng update` — `npm i -D` is the reliable route either way. Leave TypeScript alone (`~5.8.3` is what v20 wants).

Library peer deps — `npm pkg` acts on the cwd's package.json, so cd in first:

```bash
cd projects/ng-wings
npm pkg get peerDependencies              # see what's actually declared
npm pkg set 'peerDependencies.@angular/core=^20.0.0'
npm pkg set 'peerDependencies.@angular/common=^20.0.0'
npm pkg set version=20.0.0
cd ../..
```

Repeat the `peerDependencies.*` line for each `@angular/*` entry that `get` returned.

```bash
rm -rf node_modules package-lock.json
npm install --ignore-scripts
```

## Phase 3

```bash
npm run build            # library first — fix everything here
npm run build:showcase   # then downstream
```

## Phase 4 (one commit per line)

```bash
npm i primeng@20 @primeuix/themes
npm i primeicons@latest
npm i @fortawesome/angular-fontawesome@^2
npm i @ngx-translate/core@^17
```

Then the component sweep (`p-dropdown`→`p-select`, `p-calendar`→`p-datepicker`, `p-inputSwitch`→`p-toggleswitch`, `p-overlayPanel`→`p-popover`, `p-sidebar`→`p-drawer`, `p-messages`→`p-message`, TabView→Tabs) and the theming import move.

Patch decision:

```bash
# still needed in 20.x?
npx patch-package primeng            # regenerate against installed 20.x
git check-attr text -- patches/primeng+20.x.patch   # confirm eol=lf
# or, if upstream fixed it:
npm uninstall patch-package && rm -rf patches/
```

## Phase 5

```bash
npm i -D jest-preset-angular@^15 ts-jest@latest @types/jest@latest
npm test 2>&1 | tee ../post-test.log
diff ../base-test.log ../post-test.log
```

Known breaks: `TestBed.flushEffects()` → `TestBed.tick()`, and any assertion on `ng-reflect-*`.

## Phase 6 — defer

ESLint 9 / flat config and the control-flow migration are separate PRs. Pin as-is.

## Phase 7

```bash
npm ci && npm run build && npm test && npm run lint && npm run build:showcase
npm publish                    # ng-wings@20.0.0 → internal registry
npm audit                       # last, on a green tree — never --force mid-migration
```

wings-front's 19→20 branch starts only after that publish lands.
