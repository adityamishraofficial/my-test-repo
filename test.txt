Use this updated code in your component.

1. Update decorateOrderedList(...)

Replace your current method with this:

private decorateOrderedList(
  list: HTMLOListElement,
  parentPath: Array<{ index: number; style: OrderedListStyle }>
): void {
  // Get the list style type
  const listStyle = this.getOrderedListStyle(list);

  // Get start attribute, default 1
  const start = parseInt(list.getAttribute('start') || '1', 10);

  // ✅ keep start/type on the ol itself
  list.setAttribute('start', String(start));

  const typeMap: Record<OrderedListStyle, string> = {
    decimal: '1',
    'decimal-leading-zero': '1',
    'lower-alpha': 'a',
    'upper-alpha': 'A',
    'lower-roman': 'i',
    'upper-roman': 'I'
  };

  const olType = typeMap[listStyle];
  if (olType) {
    list.setAttribute('type', olType);
  }

  // Optional: also keep inline list-style-type for saved HTML
  list.style.listStyleType = listStyle;

  // Remove default list styling because custom marker is rendered manually
  list.style.listStyle = 'none';
  list.style.paddingLeft = '0';
  list.style.margin = parentPath.length === 0 ? '0 0 1rem' : '0.35rem 0 0';

  // Get all list items
  const items = Array.from(list.children).filter(
    (child): child is HTMLLIElement => child instanceof HTMLLIElement
  );

  items.forEach((item, index) => {
    const currentIndex = start + index;
    const currentPath = [...parentPath, { index: currentIndex, style: listStyle }];

    // ✅ keep start/type on li too if you want to inspect/export later
    item.setAttribute('data-list-index', String(currentIndex));
    item.setAttribute('data-list-type', olType);

    item.style.display = 'block';
    item.style.margin = '0.35rem 0';

    const childNodes = Array.from(item.childNodes);

    const nestedLists = childNodes.filter(
      (node): node is HTMLElement =>
        node instanceof HTMLOListElement || node instanceof HTMLUListElement
    );

    const contentNodes = this.normalizeContentNodes(
      childNodes.filter(
        (node) =>
          !(node instanceof HTMLOListElement) && !(node instanceof HTMLUListElement)
      )
    );

    item.replaceChildren();
    item.style.listStyle = 'none';
    item.style.margin = '0.35rem 0';
    item.style.padding = '0';

    const row = document.createElement('div');
    row.style.display = 'table';
    row.style.width = '100%';
    row.style.tableLayout = 'fixed';

    const marker = document.createElement('span');
    marker.textContent = `${this.formatMarkerPath(currentPath)}.`;
    marker.style.display = 'table-cell';
    marker.style.width = `${Math.max(3.4, currentPath.length * 1.15 + 2.1)}rem`;
    marker.style.paddingRight = '0.6rem';
    marker.style.verticalAlign = 'top';
    marker.style.textAlign = 'right';
    marker.style.fontWeight = '700';
    marker.style.color = '#1f2a37';

    const body = document.createElement('div');
    body.style.display = 'table-cell';
    body.style.verticalAlign = 'top';
    body.style.width = 'auto';
    body.style.wordBreak = 'break-word';

    for (const node of contentNodes) {
      body.appendChild(node);
    }

    if (!body.innerHTML.trim()) {
      body.innerHTML = '&nbsp;';
    }

    row.append(marker, body);
    item.appendChild(row);

    for (const nestedList of nestedLists) {
      if (nestedList instanceof HTMLOListElement) {
        this.decorateOrderedList(nestedList, currentPath);
      } else if (nestedList instanceof HTMLUListElement) {
        nestedList.style.margin = '0.35rem 0 0 1.5rem';
      }
      item.appendChild(nestedList);
    }
  });
}


⸻

2. Update getOrderedListStyle(...)

Replace your current method with this:

private getOrderedListStyle(list: HTMLOListElement): OrderedListStyle {
  const inlineStyle = list.style.listStyleType?.trim().toLowerCase();

  if (this.isSupportedOrderedListStyle(inlineStyle)) {
    return inlineStyle;
  }

  const type = list.getAttribute('type');

  switch (type) {
    case 'a':
      return 'lower-alpha';
    case 'A':
      return 'upper-alpha';
    case 'i':
      return 'lower-roman';
    case 'I':
      return 'upper-roman';
    default:
      return 'decimal';
  }
}


⸻

3. If you want start and type to be added before preview rendering

Inside your buildPreviewHtml(content: string): string, keep this:

private buildPreviewHtml(content: string): string {
  const root = document.createElement('div');
  root.innerHTML = content;

  root.style.fontFamily = 'Georgia, serif';
  root.style.fontSize = '16px';
  root.style.lineHeight = '1.6';
  root.style.color = '#1f2a37';

  this.applyElementStyles(root);
  this.decorateOrderedLists(root);

  return root.innerHTML;
}

This is fine already.

⸻

4. TinyMCE config update

Your extended_valid_elements should include li also, not only ol.

Change it to:

extended_valid_elements:
  'style[type],ol[class|style|start|type],ul[class|style],li[class|style|value|data-list-index|data-list-type],button[onclick|class|style|id|type],span[id|class|style],div[class|style|data-*]',

And keep:

valid_elements: '*[*]',
advlist_number_styles: 'default,lower-alpha,lower-roman,upper-alpha,upper-roman',


⸻

5. If you want a small helper for OrderedListStyle

Make sure your type supports all values:

type OrderedListStyle =
  | 'decimal'
  | 'decimal-leading-zero'
  | 'lower-alpha'
  | 'upper-alpha'
  | 'lower-roman'
  | 'upper-roman';


⸻

What this gives you

Now your generated HTML will preserve:

<ol start="3" type="A" style="list-style-type: upper-alpha;">

And each li will also carry:

<li data-list-index="3" data-list-type="A">

So your preview logic still works, and the original ordered-list metadata is preserved.

If you want, I can now give you one full merged copy-paste version of all related methods together.
