You are writing unit tests for an Angular project (Karma + Jasmine).

Target: @<path/to/file.ts>
Existing spec (if any): @<path/to/file.spec.ts>

Steps:
1. Read the target file and list every method/getter/setter and every branch
   (if/else, ternaries, ?., ??, switch cases, early returns, catch blocks,
   loop-empty vs loop-populated).
2. Read the existing spec and the coverage report to identify what is already
   covered. Do NOT duplicate existing tests — only add what's missing.
3. Follow the test patterns already used in this repo (TestBed setup, mock
   style, import paths). Grep a nearby *.spec.ts first instead of inventing
   a style.
4. Mock all injected dependencies with `jasmine.createSpyObj` or minimal stub
   providers. No real HTTP (use `HttpClientTestingModule`), no real store —
   provide a mock `Store` whose `select`/`pipe` returns `of(...)`.
5. Write one `describe` per method, with a test per branch. Include
   null/undefined/empty-array inputs and error paths explicitly.
6. For async code use `fakeAsync` + `tick()` (or `waitForAsync` for template
   compilation), and call `fixture.detectChanges()` where template bindings
   are asserted.

Rules:
- Do NOT modify the source file. If a branch is untestable without changing
  it, list it at the end under "Unreachable — needs refactor" and explain why.
- No `any` casts unless unavoidable; prefer typed partial mocks.
- Use Jasmine APIs only (`spyOn`, `jasmine.createSpyObj`, `.and.returnValue`),
  never Jest (`jest.fn`, `jest.spyOn`, `mockReturnValue`).
- Every spy on a void method must still be asserted with
  `toHaveBeenCalledWith`, not just `toHaveBeenCalled`, where args matter.

Output the complete updated spec file, then the command to verify:
`ng test --code-coverage --include=<path/to/file.spec.ts> --watch=false`
