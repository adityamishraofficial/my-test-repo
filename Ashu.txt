Locate the preview builder. Search for: data-rendered-preview,
   'overflow-wrap', 'min-width: 0px', 'table-layout: fixed', offsetWidth,
   getBoundingClientRect, DOMParser, and any *preview* service/util that
   transforms the editor HTML before the backend call.
2. Report the exact function and where the width/colgroup values are
   recomputed.
3. Fix it to preserve author geometry:
   - set table width only when style.width is absent
   - never emit width:100% on a table that has margin-left:auto or
     margin-right:auto (alignment requires free horizontal space)
   - keep existing <col> widths; distribute evenly only when every col
     lacks a width
   - stop measuring the DOM; read declared style values from the parsed
     HTML instead
   - keep the non-geometry print styles (overflow-wrap, vertical-align)
4. Confirm whether stripping <tr> height was intentional; if not, preserve it.
5. Verify the backend does not apply its own table-width normalization
   after receiving this HTML.
