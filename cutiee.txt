You can give Qwen the following prompt:

⸻

Task: Identify and Fix documentStage = preview Rendering Issue

There is a rendering issue in the Preview stage (documentStage === 'preview') that needs to be fixed without affecting any existing functionality.

Current Expected Behavior

Clause Status	Expected Output
FULLY_SATISFIED	Display only the clause content. Do not display the clause title/name.
PARTIALLY_SATISFIED	Display clause title + clause content.
PARTIALLY_DISSATISFIED	Display nothing.
FULLY_DISSATISFIED	Display nothing.

Current Issue

For documentStage === 'preview', all cases are working correctly except FULLY_SATISFIED.

Currently, when a clause is FULLY_SATISFIED, the preview displays:

* Clause Title ❌ (should not be displayed)
* Clause Content ✅

The expected behavior is:

* Clause Title ❌
* Clause Content ✅

Only the clause content should be visible.

Requirements

1. Identify where the preview rendering logic determines whether to display the clause title.
2. Fix the logic so that:
    * When documentStage === 'preview' and clause status is FULLY_SATISFIED, only the clause content is rendered.
    * For all other statuses, retain the existing behavior.
3. Do not modify the business logic used to determine clause satisfaction.
4. Do not change any backend or API logic.
5. Do not affect any functionality outside the Preview stage.
6. Ensure there are no regressions in existing rendering behavior for other document stages or clause statuses.
7. Before making changes, identify the root cause and update only the minimum amount of code necessary to resolve the issue.

The fix should be targeted, maintainable, and preserve all current functionality except for correcting the FULLY_SATISFIED preview rendering behavior.
