I need you to fix the TinyMCE table behavior in my existing Angular application.

IMPORTANT:
- Do NOT rewrite or redesign the existing editor.
- Do NOT upgrade Angular, TinyMCE, PrimeNG, or any other dependency.
- Do NOT make unrelated changes.
- First inspect the existing TinyMCE configuration, table-related CSS, and code that generates/inserts table HTML.
- Understand the existing implementation before making changes.
- Preserve all existing functionality.

PROBLEM 1 — HUGE SPACING BETWEEN WORDS IN TABLE CELLS

In some TinyMCE tables, text inside a cell has an abnormally large gap between words.

Example:
"Commission Amount Invoiced"

is displayed with large spaces between the words, as if the text is justified across the entire cell.

Find the actual root cause in the existing implementation. Check for:
- text-align: justify
- inline styles such as text-align: justify
- CSS applied to td/th/p
- TinyMCE alignment configuration
- generated HTML containing <p style="text-align: justify">
- inherited document/editor CSS

Fix this properly so normal table-cell text does NOT get unwanted word spacing.

Do NOT blindly force every piece of document content to left alignment. The application may support text alignment elsewhere. The fix should specifically prevent unwanted justification/word stretching in table cells while preserving the user's ability to intentionally align text where appropriate.

PROBLEM 2 — MANUALLY RESIZABLE TABLE COLUMNS

I want TinyMCE tables to behave more like Microsoft Word tables.

The user should be able to:
- Move the mouse over the boundary between two columns.
- See the resize cursor.
- Click and drag the column boundary.
- Increase/decrease the column width manually.
- Keep the other table content/layout stable as much as reasonably possible.

Inspect the current TinyMCE version and use configuration/options compatible with THAT VERSION.

Check whether the following options are appropriate for the installed version:

table_resize_bars
table_sizing_mode
table_column_resizing
object_resizing

Do not add options blindly if they are unsupported by the installed TinyMCE version.

PROBLEM 3 — TABLE WIDTH

I also want the overall table width to be adjustable where TinyMCE supports it.

If TinyMCE supports table-level resizing in the current version/configuration, enable it without breaking column resizing.

The editor is used for document-style/A4 content, so make sure the solution does not cause tables to overflow the editor/A4 page unnecessarily.

IMPORTANT A4/DOCUMENT REQUIREMENTS:
- Do not break the existing A4 page layout.
- Do not introduce horizontal scrolling unnecessarily.
- Do not cause tables to overflow the printable page.
- Preserve existing table borders, fonts, padding, and styling.
- Do not change PDF/DOCX export behavior unless required for the fix.
- Preserve existing page-break functionality.

IMPLEMENTATION PROCESS:

1. Inspect package.json/package-lock and determine the exact TinyMCE version.
2. Find the TinyMCE Angular component/configuration.
3. Find all table-related CSS.
4. Find where table HTML is generated, inserted, pasted, or transformed.
5. Find why text is being justified/stretched inside cells.
6. Determine the correct TinyMCE configuration for manual table/column resizing for the installed version.
7. Implement the smallest clean fix.
8. Search the entire project for conflicting CSS/configuration that could override the fix.
9. Check for duplicate TinyMCE configurations.
10. Run the project's build/type-check/lint/test commands that are appropriate.
11. Fix any errors introduced by your changes.
12. Do not modify unrelated files.

EXPECTED RESULT:

Before:
| Commission Amount Invoiced       |
|                                   |
The words may appear excessively spaced.

After:
| Commission Amount Invoiced |
|                            |

And for column resizing:

| Column A | Column B | Column C |
          ↑
      user can drag this boundary left/right

The user should be able to manually adjust the table column widths directly inside TinyMCE.

IMPORTANT:
Before changing anything, inspect the existing implementation and explain briefly:
- Which file(s) contain the TinyMCE configuration.
- What is causing the word-spacing problem.
- What is currently preventing/limiting table resizing.

Then implement the fix.

Do not just tell me what code I should change. Make the changes directly in the existing project.
